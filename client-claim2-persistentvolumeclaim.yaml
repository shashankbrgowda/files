apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: client-claim2
  name: client-claim2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
